# Optional — Google Sheet (published as CSV) backing the live impact counter strip.
# Leave blank to use the fallback values in public/data/impact.json.
# Format: A1=total_cases, A2=total_saved_inr (or any 2x1 range — first two numbers are read).
PUBLIC_IMPACT_CSV_URL=
